# Copy this file to `.env` (which is gitignored) and fill in your values.
# devmap reads ANTHROPIC_API_KEY at agent startup via dotenv from the
# workspace root. The agent gracefully degrades to placeholder summaries
# when the key is missing or empty.

ANTHROPIC_API_KEY=
