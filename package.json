{
  "name": "devmap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "devmap": "pnpm -F @devmap/agent run devmap",
    "demo": "pnpm -F @devmap/agent run devmap feature visits --airplane",
    "demo:owners": "pnpm -F @devmap/agent run devmap feature owners --airplane",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "validate-schema": "pnpm -F @devmap/schema run validate"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.2"
}
